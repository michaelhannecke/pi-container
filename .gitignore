# Pi writes session history and possibly other runtime data into the
# mounted config directory. Do not version it.
pi-config/sessions/
pi-config/cache/
pi-config/logs/
# pi may write provider credentials into auth.json at runtime — keep it local.
pi-config/auth.json

# macOS cruft
.DS_Store

# Local-only docs: not pushed to the repo
CLAUDE.md
en-pi-apple-container.md
